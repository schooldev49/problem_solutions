#include <stdio.h>

int main(){
    long long n;scanf("%lld",&n);
    printf("%lld",n);
    while(n != 1){
        if (n % 2)n = n * 3 + 1;
        else n /= 2;
        printf(" %lld",n);
    }
    putchar('\n');
}